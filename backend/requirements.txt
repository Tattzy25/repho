# Backend Python dependencies
redis==4.5.1
pydantic==1.10.9
python-dotenv==1.0.0
fastapi==0.95.0
uvicorn==0.23.2

# Optional: for Postgres logging, install one of:
#   psycopg[binary]>=3.2    (recommended)
#   psycopg2-binary>=2.9.9  (legacy)
